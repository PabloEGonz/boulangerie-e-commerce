<%= render "hero" %>
<div class="pt-20">
  <h2 class="text-2xl font-bold text-center uppercase py-5">Breads</h2>
  <div class="flex gap-3 lg:gap-5 flex-wrap justify-center">
    <% @breads.each do |product| %>
      <%= render "product_card", product: product %>
    <% end %>
  </div>
</div>
