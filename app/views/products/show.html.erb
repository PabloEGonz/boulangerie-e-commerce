<div class="p2">
  <h3 class="text-3xl font-semibold text-center mb-5"><%= @product.name %></h3>
  <div data-controller="calculator" class="grid lg:grid-cols-5 gap-10">
    <%= image_tag @product.image, class: "rounded col-span-2" %>
    <%= render 'new_cart_item' %>
  </div>
</div>
